# AGENTS.md

## 项目目标

本项目是一个面向 Windows 的轻量化 Markdown 与 TeX 桌面编辑器。界面保持简洁，核心范围包括 Markdown/TeX 编辑、阅读和实时预览，以及本地文档的打开与保存。

## 工作约定

- 优先保持应用轻量、清晰、易维护，避免引入与核心目标无关的复杂功能。
- 界面设计参考 Typora 的克制风格，但不复制其品牌资源或专有代码。
- 文件修改应保护用户已有内容，并在交付前运行适用的检查或构建验证。
- Windows 是首要运行平台；命令和文档应提供 Windows 可直接使用的方式。
- 项目依赖默认安装在项目目录中；除非用户明确同意，不修改系统级安全策略或全局开发环境。
- 用户已授权 Codex 无需逐次确认即可对本项目已配置的 Git 远端执行常规拉取、获取和推送操作。强制推送、删除远端分支、改写远端历史等破坏性操作仍须另行明确确认。

## 多代理分工

- 主代理负责需求理解、架构取舍、风险判断、任务拆分、代码审查与最终验收等决策性工作。
- 主代理应根据工作内容、代码复杂度、并行收益以及对可用模型能力的了解，自主选择是否委派子代理，并在 `sol`、`terra`、`luna` 中选择合适的模型类型和思考强度，使其与具体任务匹配。
- 对边界清晰、可独立实现和验证的代码任务，允许子代理直接修改代码并完成局部验证；主代理不必将子代理限制为只读分析，但必须在最终交付前审查改动、处理冲突并执行整体检查。
- 在保证实现质量的前提下，优先使用足以完成任务的最低成本模型与思考强度；仅在架构决策、复杂缺陷、关键审查或较弱配置无法可靠完成时升级模型或思考强度。
- 高复杂度、强推理或关键决策工作优先使用能力更强且思考强度更高的配置；大量边界清晰的代码实现可使用均衡配置；简单、低风险、可独立验证的工作可使用更轻量的配置。不得仅为使用子代理而拆分任务。
- 主代理始终对所有子代理产出负责；合并前必须检查其修改，处理共享工作区冲突，并执行与改动相适应的验证。

## Release 流程

- 本仓库固定使用 GitHub 远端 `origin`（`ex-asbable/Mori-Markdown`）；`.github/workflows/release.yml` 会在推送 `v*` 标签后自动检查、测试、构建 Windows 安装版与便携版，并创建正式 GitHub Release。发布时不要再探测远端类型、检查 `gh` 是否安装或重新分析工作流。
- 发布版本必须同时更新 `package.json`、`package-lock.json` 根版本及 README 中的示例产物版本，并确保标签 `vX.Y.Z` 与包版本 `X.Y.Z` 完全一致。
- 发布前只执行必要的紧凑检查：确认工作区改动范围与本地/远端 `main` 同步、目标标签不存在，运行 `npm.cmd run check`、`npm.cmd test` 和 `git diff --check`。除非发布内容涉及打包配置、依赖或资源收录，默认不在本地重复执行 `npm.cmd run dist`，由 Release 工作流完成构建。
- 验证通过后，将本次版本内容提交到 `main`，创建带说明标签 `vX.Y.Z`，依次推送 `main` 和该标签；禁止强制推送、覆盖已有标签或改写发布历史。
- 标签推送后必须等待 Release 工作流完成，并确认正式 Release（非 draft、非 prerelease）包含安装版、便携版和 `SHA256SUMS.txt`。本机没有 `gh`，直接使用已配置的 GitHub 凭据调用 Actions/Release API；凭据只在进程内读取，绝不输出或写入文件。
- 为最小化发布过程的 token 消耗：优先使用单次组合命令和简短结构化输出；Git、测试和 API 查询只返回结论、失败信息、提交 SHA、工作流状态及资产名称；不重复输出完整 diff、日志、API 响应或已经确认的信息；工作流轮询使用合理间隔，只在状态变化、失败或最终完成时汇报。

## AGENTS.md 维护授权

用户明确授权 Codex 在项目工作的任何阶段，根据实际需要随时修改本 `AGENTS.md`，以补充或调整能帮助当前及以后工作的项目约定、架构决策、验证方式和维护说明。

修改本文件时应遵守：

- 修改必须服务于本项目的持续开发和维护。
- 不得借此扩大用户原始需求或绕过安全、权限与确认要求。
- 新约定应具体、简洁，并与用户最新指令保持一致；发生冲突时以用户最新指令为准。

## 当前技术决策

- 使用现有 Node.js 环境开发，避免要求用户安装缺失的 .NET SDK 或 Rust 工具链。
- PowerShell 环境中使用 `npm.cmd`，不更改系统执行策略。
- 桌面外壳采用 Electron；渲染层使用原生 HTML、CSS 和 JavaScript，减少框架开销。
- Markdown 与 TeX 渲染使用成熟的本地依赖，应用运行时不依赖网络。
- Electron 应保持在 npm 安全审计无已知漏洞的版本；升级后运行语法检查和窗口截图冒烟测试。
- 分栏预览使用最右侧唯一的主滚动条；以 Markdown 顶层内容块建立源码与预览的语义滚动锚点，并在相邻锚点间插值，使两侧视口中心对应同一个标题、段落、列表、代码块或公式。不要退化为简单的全文高度百分比同步，也不要显示左右面板自身的纵向滚动条。
- 关闭自动换行后，编辑模式和分栏预览的左侧编辑区都必须按最长行扩宽并显示底部水平滚动条；分栏中的纵向滚动仍只由最右侧主滚动条控制。
- 左侧语法高亮层必须在连续键入或删除期间逐帧刷新，不得使用只有停止输入后才触发的尾端防抖；高亮层与 textarea 的宽度、换行和滚动位置必须保持一致。
- 输入内容和自动调整编辑区高度时必须保留当前视口位置，不得把当前行自动推到可视区域最后一行，也不得把浏览器为光标产生的内部滚动误当成用户滚动。
- 撤销/重做使用差量历史：连续、相邻、同方向且间隔不超过 1 秒的键入或删除合并为一组；光标/选区移动、粘贴、换行、Tab 和输入法上屏等操作建立撤销边界，并保留 `Ctrl+Y` 与 `Ctrl+Shift+Z` 重做。
- 未保存会话写入 `app.getPath('userData')/session.json`；窗口关闭时静默刷新并在下一次非文件关联启动时恢复。不得为该恢复机制重新引入关闭确认。
- 未命名文档的普通保存写入应用旁（开发态为项目根目录）的 `Mori Repository`；另存为必须始终显式选择位置。
- 本地图片和本地链接支持相对于当前文档的路径、Windows 绝对路径及 `file:///` URL，并经主进程解析、确认目标为文件后再交给渲染进程；保持 `nodeIntegration: false` 与 DOMPurify 清理。
